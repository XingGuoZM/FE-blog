js基础
```
symbol、bigInt（应用场景）
let/const、instanceof/typeof
原型、原型链，作用域、作用域链
this（函数调用、call/apply/bind、方法、构造函数，new执行过程）
闭包（立即执行函数）
函数继承、函数柯里化（fn(a,b,c)、fn(a)(b,c)、fn(a)(b)(c)）
异步（setTimeout/setInterval、promise、generate、async/await）
事件循环（宏观任务、微观任务）
深克隆（JSON.parse(JSON.stringify())及其优缺点、循环引用、递归（爆栈）、lodash cloneDeep）
typescript(type/interface区别)
内存泄漏、垃圾回收
节流和防抖
跨域
```
css基础
```
盒子模型
bfc（边距坍塌、）
布局（居中、两栏、三栏）
flex（flex:1指什么）
position（relative、absolute、sticky）
css 样式隔离（css module）
重绘和回流
层叠上下文（z-index）
rem和vw方案

```
html/浏览器
```
浏览器工作原理（渲染过程）
白屏、首屏、可交互时间（DOMContentLoaded、）
重绘和回流
本地存储（localstorage、sessionStorage、cookie）
```
react
```
diff过程
hook（useCallback/useMemo差异、useState过程）
hoc（优缺点、mixins）
fiber（schedule调度器、reconciler协调器、renderer渲染器）
class Component和function Component
生命周期函数改动（getDerivedStateFromProps）
react key的作用
如何解析jsx
react的优化
setState是同步还是异步
```
vue
```
new Vue()发生了什么
数据绑定原理
$set();
$nextTick()

```
webpack和工程化
```
webpack优化点（tree-shaking、按需加载、压缩、代码切割、开启多线程happyPack）
webpack打包过程
webpack热更新实现
webpack如何实现代码分离
webpack loader/plugin
tapable（事件管理）和plugin
微前端（single-spa、qiankun）
js模块化（es module/commonjs module、require）
babel（原理）
cdn（失效后，cdn多级缓存和回源）
```
网络
```
三次握手和四次挥手
缓存
https工作原理
http状态码
http2和http3区别
preload/prefetch
cdn应用场景
从url输入到页面展示，发生了什么
```
动画
```
transform+transition
requestAnimationFrame
animation/animate
```
业务优化
```
长列表：虚拟列表（intersection observer）

```
